# Reload functions
super + Escape
	pkill -USR1 -x sxhkd

super + Return
	alacritty

super + q
	bspc node -c

super + f
	bspc node -t fullscreen

super + space
	bspc node -t floating

super + d
	dmenu_run

super + e
	bspc quit

super + t
	bspc node -t tiled
